# fastapi-postgres-clerk

**Skill answers:** FastAPI · PostgreSQL · SQLAlchemy (async) · Clerk · RBAC=no · GraphQL=no · Docker=no

## What's here

Key files generated by the skill for this combination.

```
app/
  main.py            FastAPI app factory — lifespan, middleware, routers
  core/
    config.py        Pydantic settings — typed env loading
    health.py        GET /health router
    errors.py        Global exception handlers
    logger.py        Structured logging setup
requirements.txt     Python dependencies
.env.example         Environment variables
```

## Run it

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

cp .env.example .env
# fill in DATABASE_URL and CLERK_SECRET_KEY

uvicorn app.main:app --reload --port 8000
# → http://localhost:8000
# → http://localhost:8000/docs  (Swagger UI — auto-generated)
```
